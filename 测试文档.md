# 测试结果报告

我们对系统采用**自动化测试**（`backend/tests`）与**人工测试**（模拟用户使用）相结合的方式进行了测试

## 1. 自动化测试过程与结果

### 1.1 执行方式
相关测试代码在 `backend/tests`文件夹中，运行命令：

```powershell
cd backend  # 先进入后端目录
python -m pytest tests -q # 启动测试
```

### 1.2 测试用例

自动化共 26 个 case，覆盖 `test_auth.py`（14）+ `test_surveys.py`（5）+ `test_responses.py`（5）+ `test_statistics.py`（2）。其测试步骤、输入和校验输出如下：

#### A. 认证模块（14）

1. `test_register_success_response_shape`：校验**注册成功响应结构**。
- 测试步骤：调用 `POST /auth/register`
- 输入：`{"username":"wcx_user","password":"Password123"}`
- 输出：HTTP 200，`code=0`，返回 `user` 数据

2. `test_register_duplicate_username_returns_1001`：校验**重复用户名拦截**。
- 测试步骤：连续两次注册同一用户名
- 输入：两次相同 `username/password`
- 输出：第一次 200；第二次 400 且 `code=1001`

3. `test_register_prefix_usernames_should_both_succeed`：校验**前缀相似用户名可共存**。
- 测试步骤：注册 `ddd` 与 `ddd1`
- 输入：`ddd`、`ddd1`
- 输出：两次均 HTTP 200，`code=0`

4. `test_login_success_returns_token_and_user`：校验**登录成功返回 token 与用户信息**。
- 测试步骤：先注册再登录
- 输入：`login_user/Password123`
- 输出：HTTP 200，`code=0`，`token_type=bearer`

5. `test_login_wrong_password_returns_1002`：校验**密码错误分支**。
- 测试步骤：注册后使用错误密码登录
- 输入：用户名正确，密码错误
- 输出：HTTP 400，`code=1002`

6. `test_login_nonexistent_user_returns_1004`：校验**不存在用户登录提示**。
- 测试步骤：直接登录未注册账号
- 输入：`missing_user/Password123`
- 输出：HTTP 400，`code=1004`，提示先注册

7. `test_register_and_login_long_password_with_real_hash_success`：校验**长密码与真实哈希流程**。
- 测试步骤：用 100 位密码注册并登录
- 输入：`long_pwd_user/"A"*100`
- 输出：注册和登录均 HTTP 200

8. `test_register_password_too_short_returns_400_with_precise_message`：校验**密码下限**。
- 测试步骤：提交 7 位密码注册
- 输入：`password="1234567"`
- 输出：HTTP 400，`code=3001`，返回精准文案

9. `test_register_password_min_length_8_success`：校验**密码最小边界**。
- 测试步骤：提交 8 位密码注册
- 输入：`password="12345678"`
- 输出：HTTP 200，`code=0`

10. `test_register_password_too_long_returns_400_with_precise_message`：校验**密码上限**。
- 测试步骤：提交 129 位密码注册
- 输入：`password="a"*129`
- 输出：HTTP 400，`code=3001`，返回精准文案

11. `test_register_username_too_short_returns_400_with_precise_message`：校验**用户名下限**。
- 测试步骤：提交 1 位用户名注册
- 输入：`username="a"`
- 输出：HTTP 400，`code=3001`

12. `test_register_username_min_length_2_success`：校验**用户名最小边界**。
- 测试步骤：提交 2 位用户名注册
- 输入：`username="ab"`
- 输出：HTTP 200，`code=0`

13. `test_register_username_max_length_50_success`：校验**用户名最大合法边界**。
- 测试步骤：提交 50 位用户名注册
- 输入：`username="a"*50`
- 输出：HTTP 200，`code=0`

14. `test_register_username_too_long_returns_400_with_precise_message`：校验**用户名超长**。
- 测试步骤：提交 51 位用户名注册
- 输入：`username="a"*51`
- 输出：HTTP 400，`code=3001`

#### B. 问卷模块（5）

15. `test_create_survey_success_with_expected_data_structure`：校验**创建问卷与文档结构**。
- 测试步骤：调用 `POST /surveys`，再检查内存 DB 文档
- 输入：标题、描述、`settings`
- 输出：HTTP 200，返回 `survey_id/status/access_code`

16. `test_add_questions_and_logic_should_be_persisted`：校验**加题与跳转逻辑持久化**。
- 测试步骤：创建问卷后 `PUT /surveys/{survey_id}` 写入题目
- 输入：含 `logic.rules` 的题目数组
- 输出：HTTP 200，返回 3 题及逻辑字段

17. `test_publish_then_edit_should_be_forbidden`：**校验发布后禁止编辑**。
- 测试步骤：创建后发布，再尝试更新标题
- 输入：`POST /publish` 后 `PUT /surveys/{id}`
- 输出：更新请求 HTTP 403，`code=2002`

18. `test_my_surveys_should_only_return_current_user_data`：**校验用户隔离**。
- 测试步骤：用户A创建问卷，切换用户B创建问卷，查询 `/surveys/my`
- 输入：两名不同用户
- 输出：仅返回当前用户问卷

19. `test_get_public_survey_should_block_expired_survey`：校验**过期问卷不可访问**。
- 测试步骤：创建过期截止时间问卷并发布，调用公开访问接口。
- 输入：`deadline=当前时间-1天`。
- 输出：HTTP 400，及错误码`code=2004`

#### C. 提交模块（5）

20. `test_submit_response_should_follow_jump_logic_and_allow_skip_non_required_path`：校验**跳转路径下的必答题计算**。
- 测试步骤：提交 `q1=opt_yes`，按逻辑跳过 `q2`，提交 `q3`。
- 输入：`answers=[q1,q3]`。
- 输出：HTTP 200，`submission_count=1`

21. `test_submit_response_missing_required_question_on_actual_path_should_fail`：校验**实际路径缺题拦截**。
- 测试步骤：提交 `q1=opt_no` 但不提交路径中的 `q2`。
- 输入：`answers=[q1,q3]`。
- 输出：HTTP 400，`code=3003`

22. `test_submit_response_number_validation_should_fail_when_integer_required`：校验**数字题整数规则**。
- 测试步骤：整数约束题提交小数 `3.5`。
- 输入：`q3=3.5`。
- 输出：HTTP 400，`code=3001`，提示必须为整数

23. `test_submit_response_should_reject_duplicate_when_allow_multiple_false`：校验**重复提交限制**。
- 测试步骤：同一用户连续提交两次。
- 输入：两次合法答案。
- 输出：第一次 200，第二次 400 且 `code=3002`

24. `test_submit_response_should_reject_anonymous_choice_when_not_allowed`：校验**匿名策略**。
- 测试步骤：创建不允许匿名问卷，提交时设置 `is_anonymous=true`。
- 输入：`allow_anonymous=false` + `is_anonymous=true`。
- 输出：HTTP 400，`code=3001`

25. `test_get_survey_statistics_should_aggregate_all_question_types`：校验**问卷整体统计聚合**。
- 测试步骤：两名用户提交实名/匿名答卷，查询 `/surveys/{id}/statistics`。
- 输入：单选、多选、文本、数字四类答案。
- 输出：HTTP 200，`total_responses=2`，返回分题统计。
- 结果：通过，计数、文本、平均值等均正确。

26. `test_get_question_statistics_should_include_respondent_visibility_rules`：校验**单题 respondent 展示规则**。
- 测试步骤：提交实名+匿名两条记录，查询单题统计。
- 输入：同一选项下混合实名匿名。
- 输出：HTTP 200，`respondents` 含 2 条展示信息。
- 结果：通过，实名显示用户名，匿名显示“匿名用户”。

### 1.3 自动化测试的修复与结果

测试结果出现如下所示的 `DeprecationWarning`：表示我们原先在问卷deadline时间相关中写的`datetime.utcnow()` 已不推荐使用。

```bash
DeprecationWarning: datetime.datetime.utcnow() is deprecated and scheduled for removal in a future version. Use timezone-aware objects to represent datetimes in UTC: datetime.datetime.now(datetime.UTC).
now = datetime.utcnow()
```

于是将后端服务层（`services/response_service.py`和`services/survey_service.py`文件）中的 UTC 时间获取统一改为**时区感知写法**（UTC-aware）：

- `datetime.utcnow()` -> `datetime.now(timezone.utc)`

不过直接这样修复后重新启动发现**后端报错**，原因时我只把用户访问的时间在后端改为了UTC-aware的写法，mongoDB数据库存的创建者设立的deadline时间在存入时**仍然没有记录时区信息**，导致用户填写时间（时区感知时间）和创建者设立deadline时间（无时区信息时间）之间在比较时出错，于是我更改了`backend/app/database.py`中的deadline时间设置，增加时区感知的配置，更加完善：

```python
_client = MongoClient(settings.MONGODB_URI, tz_aware=True, tzinfo=timezone.utc)
```

同时在`backend/app/services/survey_service.py`和`backend/app/services/response_service.py`中分别添加`_to_utc_aware()`函数，保证deadline时间数据入库前统一为 UTC-aware

修复后系统无报错且自动化测试重新执行通过（`26 passed`），原 `utcnow()` 相关 warning 不再出现。（可见自动化测试可以帮助我们高效发现系统不易察觉的细节bug！）

![image-20260403001840429](doc_pic/test.png)

## 2. 人工测试过程与结果

人工测试就是手动模拟用户可能的行为并人工判定系统响应是否正确，实际上贯穿我们开发该问卷系统功能的始终，用于及时验证AI回复和代码逻辑是否正确，下面仅展示两个后期的测试与修复：

### 2.1 修复项一：匿名答卷泄露 respondent_id + 前端错误分组

由于第一阶段需求中写的“填写问卷都要先登录”，但是又要求能够匿名回答，需要达到一种“**系统得知而创建者不知**”的效果。按照这个需求和逻辑实现后，我们按照以下流程进行**人工测试**：

- 使用同一个账号先后用实名和匿名回答同一份问卷
- 进入统计页“答卷列表”视图
- 检查分组与用户名显示

结果发现同**账号匿名与实名答卷被合并**，即若该组最新记录为匿名，整组显示会被“匿名用户”覆盖，影响实名记录展示。

**原因在于**：即使是“匿名回复”，后端答卷列表仍返回真实 `respondent_id`，于是前端按 `respondent_id` 分组后，会将前后同一个`respondent_id`合并为同一个记录，于是先前的实名记录和匿名记录就分不开了。

**修复如下**：

1. 后端修复（`backend/app/services/response_service.py`）
- 在 `get_response_list` 组装返回数据时：
- 匿名答卷返回 `respondent_id: None`。
- 实名答卷才返回真实 `respondent_id` 与 `respondent_name`。

2. 前端修复（`frontend/src/components/StatisticsView.tsx`）
- 调整 `groupedResponses`：
- 匿名答卷按 `response_id` 单独分组。
- 实名答卷才按 `respondent_id` 分组。

**修复后效果**：

- 匿名记录不再泄露真实 `respondent_id`。
- 匿名答卷与实名答卷不再被错误合并。
- 实名记录用户名显示正常，匿名记录显示“匿名用户”。

![image-20260403004637786](doc_pic/test_fix.png)

### 2.1 修复项二：编辑问卷选项时两个选项同步变化

在“题目编辑”页面，系统支持创建者用户调整题目选项，然而在人工测试流程测试该功能时，我们发现：

- 打开问卷编辑页，保证题目有至少 2 个选项
- 删除第 1 个选项，再新增一个选项
- 修改现在的两个选项的其中一个选项
- **两个选项一起同步被修改**

**原因在于**：删除选项 1 后再新增选项，可能生成与已有项重复的 `option_id`。在 React 列表渲染中，重复 key 会导致编辑一个选项时另一个选项同步变化。

**修复如下**：

文件：`frontend/src/components/SurveyEditor.tsx`
- 原逻辑：`option_id: opt${opts.length + 1}`（存在碰撞风险）。
- 修复后：`option_id` 改为 `opt_${时间戳36进制}_${随机片段}`，保证新增选项 ID 唯一。

**修复后效果**：

- 选项不再出现同步联动修改。
- 每个选项可独立编辑，渲染稳定。
