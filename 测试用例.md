# 第一阶段测试用例（自动化 + API）

说明：以下测试用例均已在 `pytest` 中实现，对应文件为 `test_surveys.py`、`test_responses.py`、`test_statistics.py`。

## 1. 创建问卷测试

### 用例 1.1 创建问卷成功并校验数据结构

- 测试步骤

1. 调用 `POST /surveys` 提交标题、描述、settings。
2. 检查返回体 `code/message/data` 结构。
3. 检查数据库 `surveys` 文档字段完整性。

- 输入

1. `title=课程反馈`
2. `settings.allow_anonymous=true`
3. `settings.allow_multiple=false`

- 输出

1. HTTP 200
2. `code=0`
3. 返回 `survey_id/access_code/status/created_at`

- 结果

1. 新问卷状态为 `draft`
2. `questions=[]`
3. `response_count=0`

## 2. 添加题目测试

### 用例 2.1 编辑问卷并添加多题型题目

- 测试步骤

1. 先创建问卷。
2. 调用 `PUT /surveys/{survey_id}` 写入题目数组。
3. 校验题目顺序、题型、校验字段和逻辑字段持久化。

- 输入

1. 单选题 `q1`
2. 文本题 `q2`
3. 数字题 `q3`

- 输出

1. HTTP 200
2. 返回问卷包含 3 道题

- 结果

1. `question_id/type/order` 保持一致
2. `logic.rules` 和 `validation` 正确保存

## 3. 跳转逻辑测试

### 用例 3.1 跳转后跳过非路径必填题

- 测试步骤

1. 创建并发布含跳转规则问卷（`q1(opt_yes)->q3`）。
2. 提交答案仅包含 `q1=opt_yes` 与 `q3`。
3. 检查提交是否成功。

- 输入

1. `q1=opt_yes`
2. `q3=5`

- 输出

1. HTTP 200
2. `code=0`

- 结果

1. 服务端按路径动态计算必答题
2. `q2` 不在路径中时不会误判必填缺失

### 用例 3.2 实际路径缺少必填题应失败

- 测试步骤

1. 使用同一问卷提交 `q1=opt_no`（顺序进入 `q2`）。
2. 故意不提交 `q2`。
3. 检查错误码。

- 输入

1. `q1=opt_no`
2. 缺失 `q2`

- 输出

1. HTTP 400
2. `code=3003`

- 结果

1. 返回"必填题未回答"错误

## 4. 校验测试

### 用例 4.1 数字题整数约束校验

- 测试步骤

1. 创建并发布包含 `integer_only=true` 的数字题。
2. 提交小数答案。
3. 检查错误码与错误文案。

- 输入

1. `q3=3.5`

- 输出

1. HTTP 400
2. `code=3001`

- 结果

1. 返回"必须为整数"

### 用例 4.2 匿名策略校验

- 测试步骤

1. 创建 `allow_anonymous=false` 问卷并发布。
2. 提交时设置 `is_anonymous=true`。
3. 检查业务错误。

- 输入

1. `is_anonymous=true`

- 输出

1. HTTP 400
2. `code=3001`

- 结果

1. 返回"该问卷不允许匿名填写"

### 用例 4.3 文本题最小长度校验

- 测试步骤

1. 创建并发布包含 `min_length=5` 的文本题。
2. 提交少于 5 个字符的答案。
3. 检查错误码与错误文案。

- 输入

1. `q1=短`（1 个字符）

- 输出

1. HTTP 400
2. `code=3001`

- 结果

1. 返回"最少"或"至少"相关错误提示

### 用例 4.4 文本题最大长度校验

- 测试步骤

1. 创建并发布包含 `max_length=10` 的文本题。
2. 提交超过 10 个字符的答案。
3. 检查错误码与错误文案。

- 输入

1. `q1=这是一段超过十个字的长文本内容`

- 输出

1. HTTP 400
2. `code=3001`

- 结果

1. 返回"最多"或"不超过"相关错误提示

### 用例 4.5 多选题最少选择数量校验

- 测试步骤

1. 创建并发布包含 `min_selected=2` 的多选题。
2. 提交仅选择 1 个选项的答案。
3. 检查错误码与错误文案。

- 输入

1. `q1=[opt1]`（仅 1 个选项）

- 输出

1. HTTP 400
2. `code=3001`

- 结果

1. 返回"至少"相关错误提示

### 用例 4.6 多选题最多选择数量校验

- 测试步骤

1. 创建并发布包含 `max_selected=2` 的多选题。
2. 提交选择 3 个选项的答案。
3. 检查错误码与错误文案。

- 输入

1. `q1=[opt1, opt2, opt3]`（3 个选项）

- 输出

1. HTTP 400
2. `code=3001`

- 结果

1. 返回"最多"相关错误提示

### 用例 4.7 数字题最小值校验

- 测试步骤

1. 创建并发布包含 `min_value=0` 的数字题。
2. 提交小于最小值的答案。
3. 检查错误码与错误文案。

- 输入

1. `q1=-5`

- 输出

1. HTTP 400
2. `code=3001`

- 结果

1. 返回"最小值"或"不能小于"相关错误提示

### 用例 4.8 数字题最大值校验

- 测试步骤

1. 创建并发布包含 `max_value=120` 的数字题。
2. 提交大于最大值的答案。
3. 检查错误码与错误文案。

- 输入

1. `q1=150`

- 输出

1. HTTP 400
2. `code=3001`

- 结果

1. 返回"最大值"或"不能大于"相关错误提示

## 5. 提交问卷测试

### 用例 5.1 重复提交限制

- 测试步骤

1. 创建 `allow_multiple=false` 问卷并发布。
2. 同一用户提交两次。
3. 校验第二次返回。

- 输入

1. 第一次合法答案
2. 第二次合法答案

- 输出

1. 第一次 HTTP 200
2. 第二次 HTTP 400 + `code=3002`

- 结果

1. 正确拦截重复提交

## 6. 统计测试

### 用例 6.1 问卷整体统计（单选/多选/文本/数字）

- 测试步骤

1. 创建多题型问卷并发布。
2. 两个不同用户分别提交实名与匿名答卷。
3. 调用 `GET /surveys/{survey_id}/statistics`。
4. 校验各题聚合结果。

- 输入

1. 单选：`opt_py`/`opt_js`
2. 多选：`[opt_git,opt_ci]`、`[opt_git]`
3. 文本：两条字符串
4. 数字：3 与 5

- 输出

1. HTTP 200
2. `total_responses=2`
3. 返回每题统计对象

- 结果

1. 计数正确：`opt_git=2`
2. 文本列表包含两条输入
3. 数字统计 `average=4.0, min=3, max=5`

### 用例 6.2 单题统计中的实名/匿名显示规则

- 测试步骤

1. 创建问卷并提交一条实名、一条匿名答卷。
2. 调用 `GET /surveys/{survey_id}/questions/{question_id}/statistics`。
3. 检查 respondents 列表。

- 输入

1. `q_single=opt_py`

- 输出

1. HTTP 200
2. `option_statistics[].respondents` 含 2 条记录

- 结果

1. 实名用户显示用户名
2. 匿名用户显示 `匿名用户`，且不暴露 `respondent_id`

## 7. 用户认证测试

### 用例 7.1 注册成功并校验返回结构

- 测试步骤

1. 调用 `POST /auth/register` 提交合法用户名和密码。
2. 校验响应体 `code/message/data` 结构。
3. 校验返回用户字段与时间格式。

- 输入

1. `username=wcx_user`
2. `password=Password123`

- 输出

1. HTTP 200
2. `code=0`
3. `data.username=wcx_user`

- 结果

1. 注册成功并返回标准结构

### 用例 7.2 重复用户名注册应失败

- 测试步骤

1. 使用同一用户名连续注册两次。
2. 校验第二次响应。

- 输入

1. `username=same_name`
2. `password=Password123`

- 输出

1. 第一次 HTTP 200
2. 第二次 HTTP 400 + `code=1001`

- 结果

1. 正确拦截重复用户名

### 用例 7.3 前缀相似用户名应可同时注册

- 测试步骤

1. 依次注册 `ddd` 与 `ddd1`。
2. 校验两次均成功。

- 输入

1. `username=ddd`
2. `username=ddd1`

- 输出

1. 两次均 HTTP 200
2. 两次均 `code=0`

- 结果

1. 不会将相似前缀用户名误判为重复

### 用例 7.4 登录成功返回 Token 与用户信息

- 测试步骤

1. 先注册用户。
2. 调用 `POST /auth/login` 使用正确凭据登录。
3. 校验 token 类型与用户信息。

- 输入

1. `username=login_user`
2. `password=Password123`

- 输出

1. HTTP 200
2. `code=0`
3. 返回 `token_type/access_token/user`

- 结果

1. 登录成功并返回 bearer token

### 用例 7.5 登录失败校验（密码错误/用户不存在）

- 测试步骤

1. 场景 A：注册后使用错误密码登录。
2. 场景 B：使用不存在用户名登录。
3. 校验错误码与文案。

- 输入

1. 场景 A：`username=wrong_pwd_user, password=wrong-password`
2. 场景 B：`username=missing_user, password=Password123`

- 输出

1. 场景 A：HTTP 400 + `code=1002`
2. 场景 B：HTTP 400 + `code=1004`

- 结果

1. 分别返回正确的登录失败原因

### 用例 7.6 长密码注册与登录（真实哈希）

- 测试步骤

1. 使用 100 位密码注册用户。
2. 使用同一密码登录。
3. 校验注册与登录均成功。

- 输入

1. `username=long_pwd_user`
2. `password=A*100`

- 输出

1. 注册 HTTP 200 + `code=0`
2. 登录 HTTP 200 + `code=0`

- 结果

1. 长密码在真实哈希策略下可正确处理

### 用例 7.7 用户名与密码长度边界校验

- 测试步骤

1. 校验密码长度：7 位、8 位、129 位。
2. 校验用户名长度：1 位、2 位、50 位、51 位。
3. 校验成功场景与失败场景的错误码和提示文案。

- 输入

1. 密码：`1234567`、`12345678`、`a*129`
2. 用户名：`a`、`ab`、`a*50`、`a*51`

- 输出

1. 非法长度均 HTTP 400 + `code=3001`
2. 合法边界长度注册成功（HTTP 200 + `code=0`）

- 结果

1. 长度约束与提示文案符合预期

## 8. 问卷管理与访问控制测试

### 用例 8.1 发布后编辑应被禁止

- 测试步骤

1. 创建问卷并发布。
2. 再次调用 `PUT /surveys/{survey_id}` 进行编辑。
3. 校验权限错误。

- 输入

1. 已发布问卷
2. 尝试修改 `title`

- 输出

1. HTTP 403
2. `code=2002`

- 结果

1. 已发布问卷不可再编辑

### 用例 8.2 我的问卷列表应进行用户隔离

- 测试步骤

1. 用户 A 创建问卷。
2. 切换到用户 B 创建问卷。
3. 调用 `GET /surveys/my` 校验只返回当前用户数据。

- 输入

1. 两个不同用户分别创建问卷

- 输出

1. HTTP 200
2. `surveys` 仅包含当前登录用户创建的数据

- 结果

1. 用户间数据隔离正确

### 用例 8.3 过期问卷应禁止公共访问

- 测试步骤

1. 创建截止时间已过的问卷并发布。
2. 调用 `GET /public/surveys/{access_code}`。
3. 校验过期拦截错误。

- 输入

1. `deadline=当前时间-1天`

- 输出

1. HTTP 400
2. `code=2004`

- 结果

1. 过期问卷无法通过公共入口访问

## 9. 跳转逻辑合法性测试

### 用例 9.1 非法跳转配置应失败

- 测试步骤

1. 场景 A：配置跳转到不存在题目。
2. 场景 B：配置向前跳转或跳转到自身。
3. 场景 C：构造包含环路的跳转链路。
4. 调用 `PUT /surveys/{survey_id}` 保存问卷并校验错误。

- 输入

1. `jump_to -> q999`（不存在）
2. `q3 -> q1`（向前跳转）
3. `q1 -> q1`（跳转到自身）
4. 含回跳的复杂链路

- 输出

1. HTTP 400
2. `code=3001`

- 结果

1. 服务端阻止非法跳转并返回明确错误

### 用例 9.2 合法跳转配置应成功

- 测试步骤

1. 配置仅向后跳转且无环的规则。
2. 配置多规则并跳转到不同目标。
3. 配置多路径但无循环场景。
4. 保存问卷并校验成功。

- 输入

1. `q1 -> q3`、`q3 -> q5`
2. 多条规则分别 `end_survey` 与 `jump_to q4`

- 输出

1. HTTP 200
2. `code=0`

- 结果

1. 合法逻辑可正常保存

### 用例 9.3 特殊动作与禁用逻辑校验

- 测试步骤

1. 场景 A：规则动作为 `end_survey`。
2. 场景 B：`logic.enabled=false` 且包含原本非法的跳转配置。
3. 保存问卷并校验不触发目标校验失败。

- 输入

1. `action=end_survey`
2. `enabled=false` + 向前跳转规则

- 输出

1. HTTP 200
2. `code=0`

- 结果

1. `end_survey` 与禁用逻辑按预期处理

## 10. 问卷状态管理测试

### 用例 10.1 关闭问卷成功

- 测试步骤

1. 创建问卷并发布。
2. 调用 `POST /surveys/{survey_id}/close` 关闭问卷。
3. 校验问卷状态变更。

- 输入

1. 已发布问卷

- 输出

1. HTTP 200
2. `code=0`
3. `status=closed`

- 结果

1. 问卷状态成功变更为 `closed`

### 用例 10.2 删除问卷成功

- 测试步骤

1. 创建问卷。
2. 调用 `DELETE /surveys/{survey_id}` 删除问卷。
3. 再次获取该问卷应返回 404。

- 输入

1. 待删除问卷的 `survey_id`

- 输出

1. 删除操作 HTTP 200 + `code=0`
2. 再次获取 HTTP 404

- 结果

1. 问卷成功删除且无法再访问

## 11. 答卷查看测试

### 用例 11.1 创建者获取答卷列表

- 测试步骤

1. 创建并发布问卷。
2. 其他用户提交答卷。
3. 创建者调用 `GET /surveys/{survey_id}/responses` 获取答卷列表。
4. 校验返回数据包含提交的答卷。

- 输入

1. 问卷创建者身份
2. 已有答卷的问卷 ID

- 输出

1. HTTP 200
2. `data` 为答卷列表数组
3. 列表长度 >= 1

- 结果

1. 创建者可查看问卷的所有答卷列表
2. 返回 `response_id/respondent_name/submitted_at` 等信息

### 用例 11.2 创建者获取答卷详情

- 测试步骤

1. 创建并发布问卷。
2. 其他用户提交答卷。
3. 创建者调用 `GET /responses/{response_id}` 获取答卷详情。
4. 校验返回完整答案数据。

- 输入

1. 问卷创建者身份
2. 答卷 `response_id`

- 输出

1. HTTP 200
2. 返回 `response_id/survey_id/answers`

- 结果

1. 创建者可查看答卷的完整详情
2. `answers` 数组包含所有题目的回答
