# 问卷系统 - 系统说明

## 1. 项目概述

本项目是一个基于 **MongoDB** 的在线问卷系统，目标是实现一个简化版的问卷平台，支持用户注册登录、创建问卷、发布问卷、填写问卷以及查看统计结果等核心功能。

系统重点围绕以下几个方面展开：
- 使用 MongoDB 完成问卷系统的数据建模
- 使用后端程序实现问卷创建、答卷提交、统计分析等业务逻辑
- 支持题目校验规则与跳转逻辑
- 使用 AI 工具辅助系统设计、代码生成与文档整理

本系统为课程项目第一阶段成果，后续还需要在此基础上进行第二阶段的需求变更与扩展，因此在设计时会尽量保证结构清晰、易于修改和扩展。

---

## 2. 技术栈

| 层面 | 技术 |
|------|------|
| 后端 | Python + FastAPI |
| 数据库 | MongoDB + PyMongo |
| 前端 | React + Vite + TypeScript |
| UI 组件库 | shadcn/ui + Tailwind CSS |
| 后端包管理 | pip + requirements.txt |
| 前端包管理 | npm |
| 认证 | JWT（PyJWT） |

---

## 3. 技术选型说明

### 3.1 后端：Python + FastAPI

后端采用 Python + FastAPI 进行开发。FastAPI 具有以下优点：
- 开发效率高，语法简洁，适合课程项目快速实现
- 原生支持类型标注，便于定义清晰的数据结构
- 自动生成 Swagger/OpenAPI 文档，方便调试与接口联调
- 适合实现问卷系统中的注册登录、问卷管理、答卷提交与统计接口

### 3.2 数据库：MongoDB + PyMongo

数据库采用 MongoDB，驱动选择 PyMongo。原因如下：
- 问卷、题目、校验规则、跳转逻辑都具有灵活嵌套结构，适合文档型数据库建模
- 题目嵌入问卷、答案嵌入答卷记录，能减少关联查询，提高读取效率
- MongoDB 的 schema 灵活，便于第二阶段需求变更时扩展字段
- PyMongo 使用简单直接，适合当前项目开发节奏

### 3.3 前端：React + Vite + TypeScript

前端采用 React + Vite + TypeScript。原因如下：
- React 适合实现动态表单、题目编辑器、问卷填写流程等交互较多的页面
- Vite 启动快、构建轻量，适合课程项目快速开发
- TypeScript 能提升代码可维护性，减少前后端字段对接错误

### 3.4 UI：shadcn/ui + Tailwind CSS

前端界面使用 shadcn/ui 与 Tailwind CSS 搭建。原因如下：
- shadcn/ui 组件风格简洁，适合快速搭建不复杂但规范的页面
- Tailwind CSS 可高效完成布局与样式调整
- 结合 React 使用方便，适合登录、问卷编辑、填写、统计等页面开发

### 3.5 认证：JWT（PyJWT）

系统认证采用 JWT。原因如下：
- 登录后由后端签发 token，前端后续请求携带 token 即可完成身份验证
- 适合前后端分离架构
- 便于区分需要登录访问的接口与公开访问的问卷填写接口

---

## 4. 系统功能概述

### 4.1 用户账号功能
- 用户注册（用户名、密码、邮箱）
- 用户登录（JWT 认证）
- 登录后查看自己创建的问卷

### 4.2 问卷创建与管理
- 创建问卷（标题、说明、是否允许匿名填写）
- 编辑问卷中的题目（单选题、多选题、文本填空、数字填空）
- 为题目设置校验规则（选择数量限制、字数限制、数值范围限制等）
- 为题目设置跳转逻辑（根据单选/多选/数字填空结果跳转到指定题目）
- 发布问卷 / 关闭问卷
- 设置问卷截止时间
- 通过访问码生成分享链接（`/survey/{access_code}`）

### 4.3 填写问卷
- 通过分享链接进入问卷
- 按顺序回答题目，支持跳转逻辑
- 实时校验填写内容是否符合规则
- 所有填写均需先登录，支持设置问卷的匿名结果显示模式
- 支持多人同时填写
- 支持同一用户多次提交（若问卷允许）

### 4.4 统计结果查看
- 单选题：每个选项的选择人数
- 多选题：每个选项的选择次数
- 文本填空：查看所有填写内容
- 数字填空：查看所有填写内容 + 平均值
- 查看整个问卷的统计 / 查看某一道题的统计

---

## 5. 系统模块划分

系统按功能划分为以下模块：

| 模块 | 说明 |
|------|------|
| 用户模块 | 注册、登录、JWT 认证 |
| 问卷管理模块 | 问卷 CRUD、发布/关闭、截止时间 |
| 题目编辑模块 | 题目增删改、选项管理、校验规则设置 |
| 跳转逻辑模块 | 跳转规则设置、跳转引擎执行 |
| 问卷填写模块 | 填写流程、实时校验、跳转执行、答卷提交 |
| 统计模块 | 按题型聚合统计、结果展示 |

---

## 6. 项目目录结构

```
db-project1/
├── backend/                     # FastAPI 后端
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py              # FastAPI 应用入口
│   │   ├── config.py            # 配置（MongoDB URI、JWT 密钥等）
│   │   ├── database.py          # PyMongo 数据库连接
│   │   ├── models/              # Pydantic 数据模型
│   │   │   ├── __init__.py
│   │   │   ├── user.py          # 用户模型
│   │   │   ├── survey.py        # 问卷 & 题目模型
│   │   │   └── response.py      # 答卷模型
│   │   ├── routes/              # API 路由
│   │   │   ├── __init__.py
│   │   │   ├── auth.py          # 注册登录接口
│   │   │   ├── surveys.py       # 问卷管理接口
│   │   │   ├── responses.py     # 答卷提交接口
│   │   │   └── statistics.py    # 统计接口
│   │   ├── services/            # 业务逻辑层
│   │   │   ├── __init__.py
│   │   │   ├── auth_service.py
│   │   │   ├── survey_service.py
│   │   │   ├── response_service.py
│   │   │   └── statistics_service.py
│   │   ├── middlewares/         # 中间件
│   │   │   ├── __init__.py
│   │   │   └── auth.py          # JWT 认证中间件
│   │   └── utils/               # 工具函数
│   │       ├── __init__.py
│   │       └── response.py      # 统一响应格式
│   ├── requirements.txt         # Python 依赖
│   └── tests/                   # 后端测试
│       ├── __init__.py
│       ├── test_auth.py
│       ├── test_surveys.py
│       ├── test_responses.py
│       └── test_statistics.py
├── frontend/                    # React 前端
│   ├── src/
│   │   ├── components/          # 公共组件 & shadcn 组件
│   │   ├── pages/               # 页面组件
│   │   ├── services/            # API 调用封装
│   │   ├── hooks/               # 自定义 hooks
│   │   ├── lib/                 # 工具库
│   │   ├── types/               # TypeScript 类型定义
│   │   ├── App.tsx
│   │   └── main.tsx
│   ├── package.json
│   └── ...
├── db_design.md                 # 数据库设计文档
├── project_requirements.md      # 项目需求文档
├── ai_log.md                    # AI使用过程 
└── system_docs.md               # 系统说明文档
```

---

## 7. 开发方式说明

本项目采用**前后端分离**方式开发：
- 后端负责数据存储、业务逻辑、接口实现、校验和统计处理
- 前端负责页面展示、交互逻辑、表单收集与接口调用
- 前后端通过 RESTful API 通信，使用 JSON 格式传输数据
- 认证通过 JWT token 在 HTTP 请求头中传递

---

## 8. 开发分工说明

两位成员按**功能模块**分工协作：

### 丁熙妍（创建侧）
- 用户注册 / 登录（页面 + API）
- 问卷创建 / 编辑 / 管理（页面 + API）
- 题目编辑器（添加/删除/排序题目、设置选项、校验规则）
- 问卷列表页

### 吴晨曦（填写侧 + 分析侧）
- 跳转逻辑设置界面 + 跳转引擎
- 填写问卷（页面 + API，包含校验和跳转执行）
- 提交答卷
- 统计结果查看（页面 + API）

### 公共基础设施
在分工开发前，由两人共同完成以下基础设施搭建：
- 数据库 Pydantic 模型定义
- 项目目录结构初始化
- 数据库连接模块
- JWT 认证中间件
- API 接口契约文档
- 统一响应格式
- 前端 API 封装 / 路由配置 / 基础布局
